# These files will NOT be installed/modified by the setup script.
# To add extra files, add a regular expression that will match
# the files you wish to ignore.
# Note: Only files/directories in the root of the repo are installed
# so ignoring subdirectories/nested files will have no effect.
\.git$
\.gitignore$
\.dotfiles-ignore$
\.dotfiles-once$
\.ssh$
install\.py$
